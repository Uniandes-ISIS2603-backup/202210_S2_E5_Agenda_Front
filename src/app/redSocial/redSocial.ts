export class redSocial{

  id: number;
  tipo: string;
  direccion: string;

  constructor(id: number, tipo: string, direccion: string) {
    this.id = id;
    this.tipo = tipo;
    this.direccion = direccion;
  }
}

